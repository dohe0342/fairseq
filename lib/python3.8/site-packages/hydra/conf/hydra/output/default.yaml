# @package hydra

run:
  dir: /home/work/workspace/fairseq_result/outputs/${now:%Y-%m-%d}/${now:%H-%M-%S}
sweep:
  dir: /home/work/workspace/fariseqi_result/multirun/${now:%Y-%m-%d}/${now:%H-%M-%S}
  subdir: ${hydra.job.num}
